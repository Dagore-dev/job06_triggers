create table seniority (
    emple_id int,
    amount_years int,
    PRIMARY KEY (emple_id)
);

create table employee (
    emple_id int,
    name     varchar2(10),
    salary int,
    PRIMARY KEY (emple_id)
);
